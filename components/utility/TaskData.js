export default class TaskData {
  constructor(title, description, startDate, deadLine) {
      this.title = title
      this.description = description
      this.startDate = startDate
      this.deadLine = deadLine
  }
}